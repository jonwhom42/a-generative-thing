# ===========================================
# FRONTEND (Vite) - these are public/bundled
# ===========================================

# Clerk Authentication (Frontend) - Get yours at https://dashboard.clerk.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# ===========================================
# BACKEND (Server) - these are secret
# ===========================================

# Gemini API Key (Server-side only) - Get yours at https://aistudio.google.com/apikey
# IMPORTANT: This key is NOT exposed to the browser bundle
GEMINI_API_KEY=your_server_side_gemini_key_here

# Clerk Secret Key (Server-side only) - Get from https://dashboard.clerk.com/api-keys
CLERK_SECRET_KEY=sk_test_xxx

# Optional: Frontend URL for CORS (defaults to http://localhost:5173)
# In production, set this to your actual frontend domain
# FRONTEND_URL=https://your-app.com

# Optional: Rate limiting configuration
# GEMINI_RATE_LIMIT_WINDOW_MS=60000    # Time window in ms (default: 60000 = 1 minute)
# GEMINI_RATE_LIMIT_MAX_REQUESTS=60    # Max requests per window per user (default: 60)

# Optional: Node environment (affects logging format and error details)
# NODE_ENV=production
